# allure3-docker-service

Сервис-обёртка вокруг **Allure 3** для хранения прогонов тестов по
проектам, генерации HTML-отчётов и удобного просмотра через встроенный
UI.

Сервис принимает `allure-results` через HTTP API, автоматически создаёт
проекты, инкрементирует run_id, генерирует отчёты и предоставляет
удобный UI.

------------------------------------------------------------------------

## 🚀 Возможности

-   Автосоздание проекта при первом пуше
-   Автоинкремент run_id (1,2,3...)
-   Генерация Allure 3 отчёта внутри контейнера
-   Хранение данных в `/data/projects/...`
-   UI для просмотра проектов и прогонов
-   API для загрузки, удаления и regenerate
-   Логирование запросов + x-request-id
-   Docker / Docker Compose ready

------------------------------------------------------------------------

## 🐳 Быстрый старт

``` bash
docker compose up --build
```

UI будет доступен по адресу:

http://localhost:8080/ui/

------------------------------------------------------------------------

## 📂 Структура хранения

    /data/projects/
      <project>/
        latest.json
        runs/
          1/
            allure-results/
            report/
            meta.json
            status.json

------------------------------------------------------------------------

## 🔌 API

### Загрузка результатов

POST /api/v1/projects/{project}/runs

multipart/form-data: - results --- zip с allure-results - meta --- JSON
строка (опционально)

Пример:

``` bash
zip -r allure-results.zip allure-results

curl -X POST   -F "results=@allure-results.zip"   -F 'meta={"branch":"master","commit":"abc123"}'   http://localhost:8080/api/v1/projects/demo/runs
```

------------------------------------------------------------------------

## 🧪 Интеграция с pytest

Используйте плагин:

https://github.com/BekoSa/pytest-allure3-uploader

Пример:

``` bash
pytest --alluredir=allure-results   --allure-upload   --allure-upload-url http://localhost:8080   --allure-upload-project demo
```

------------------------------------------------------------------------

## ⚙️ Переменные окружения

-   RUST_LOG --- уровень логирования
-   DATA_DIR --- директория хранения (по умолчанию /data)
-   ALLURE_BIN --- путь к allure бинарю

------------------------------------------------------------------------

## License

MIT
